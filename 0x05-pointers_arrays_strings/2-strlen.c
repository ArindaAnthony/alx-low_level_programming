#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns length of a string
 * @s: parameter
 * Return: always void
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
